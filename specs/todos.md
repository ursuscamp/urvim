# Todos

This is a list, in no particular order, of things that need to be addressed which do not have specs created yet.

## Features

- session support
- fuzzy finder
- should panes hold dynamic widgets?

- change the background job system to allow for streaming results
    - set the file picker to use it
- simple input widget can be re-used for both command line and picker
- fixed width similar to command line
- long file labels should be shortened, like "specs/in-progress/some-feature/requirements.md" like "s/i/some-feature/requirements.md"
    - if the path is still long it should truncated from the beginning with ellipses, like ...progress/some-feature/requirements.md

# Bugs

