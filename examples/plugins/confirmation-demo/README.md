# Confirmation Demo

This plugin demonstrates `urvim.ui.confirm` with custom response labels,
shortcuts, values, and cancellation handling.

Configure the plugin and run `confirmation-demo open` from urvim's command
line.
