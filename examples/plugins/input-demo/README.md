# Input Demo

This plugin demonstrates `urvim.ui.input` with a custom title, prompt, initial
text, and cancellation handling.

Configure the plugin and run `input-demo open` from urvim's command line.
